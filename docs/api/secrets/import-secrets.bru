meta {
  name: Import Secrets
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/v1/secrets/import
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file(/path/to/secrets.json)
  format: json
  overwrite: false
}
