meta {
  name: Get Secret Version
  type: http
  seq: 10
}

get {
  url: {{base_url}}/api/v1/secrets/{{secret_id}}/versions/{{version}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
