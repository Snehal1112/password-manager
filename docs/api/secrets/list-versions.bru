meta {
  name: List Secret Versions
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/secrets/{{secret_id}}/versions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
