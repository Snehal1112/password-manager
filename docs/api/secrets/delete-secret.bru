meta {
  name: Delete Secret
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/secrets/{{secret_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
