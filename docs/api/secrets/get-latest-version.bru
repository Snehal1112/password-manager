meta {
  name: Get Latest Secret Version
  type: http
  seq: 11
}

get {
  url: {{base_url}}/api/v1/secrets/{{secret_id}}/versions/latest
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
