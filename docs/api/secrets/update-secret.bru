meta {
  name: Update Secret
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/v1/secrets/{{secret_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "value": "new-s3cr3t-value",
    "tags": ["env:prod"]
  }
}
