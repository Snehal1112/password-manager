meta {
  name: Update Audit Config
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/v1/audit/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "retention_days": 365
  }
}
