meta {
  name: List Deleted Certificates
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/deleted/certificates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
