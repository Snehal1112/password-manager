meta {
  name: Purge Key (Permanent Delete)
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/v1/deleted/keys/{{key_id}}/purge
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
