meta {
  name: Restore Deleted Secret
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/deleted/secrets/{{secret_id}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
