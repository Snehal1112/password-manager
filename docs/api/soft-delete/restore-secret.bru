meta {
  name: Restore Deleted Secret
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/deleted/secrets/{{secret_id}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
