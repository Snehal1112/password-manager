meta {
  name: Restore Deleted Key
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/deleted/keys/{{key_id}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
