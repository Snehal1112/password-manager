meta {
  name: Purge Secret (Permanent Delete)
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/v1/deleted/secrets/{{secret_id}}/purge
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
