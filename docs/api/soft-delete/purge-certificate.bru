meta {
  name: Purge Certificate (Permanent Delete)
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/deleted/certificates/{{certificate_id}}/purge
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
