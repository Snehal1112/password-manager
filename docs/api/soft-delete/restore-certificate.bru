meta {
  name: Restore Deleted Certificate
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/deleted/certificates/{{certificate_id}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
