meta {
  name: Get Access Policy
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/access-policies/{{policy_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
