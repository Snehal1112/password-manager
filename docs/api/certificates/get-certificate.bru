meta {
  name: Get Certificate
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/certificates/{{certificate_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
