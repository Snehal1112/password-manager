meta {
  name: Delete Certificate
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/certificates/{{certificate_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
