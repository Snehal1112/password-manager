meta {
  name: Get Certificate Policy
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/certificates/{{certificate_id}}/policy
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
