meta {
  name: Sign Key
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/keys/{{key_id}}/sign
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "value": "<base64-encoded-data-to-sign>",
    "algorithm": "RS256"
  }
}
