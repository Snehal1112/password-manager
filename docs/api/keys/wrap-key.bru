meta {
  name: Wrap Key
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/v1/vaults/{{vault_name}}/keys/{{key_id}}/wrap
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "plaintext_key": "<base64-encoded-key-material>",
    "algorithm": "RSA-OAEP"
  }
}
