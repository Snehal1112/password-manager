meta {
  name: Delete Service Account
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/v1/service-accounts/{{service_account_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
