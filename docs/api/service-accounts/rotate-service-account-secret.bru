meta {
  name: Rotate Service Account Secret
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/v1/service-accounts/{{service_account_id}}/rotate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
