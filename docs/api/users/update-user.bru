meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/v1/users/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "updateduser"
  }
}
