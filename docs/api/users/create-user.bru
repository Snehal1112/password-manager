meta {
  name: Create User
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "testuser",
    "password": "password123",
    "roles": ["user"]
  }
}
