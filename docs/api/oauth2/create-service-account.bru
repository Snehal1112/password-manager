meta {
  name: Create Service Account
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/service-accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "my-service-account",
    "description": "CI/CD pipeline service account"
  }
}
